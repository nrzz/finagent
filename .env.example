# Copy to .env only if you need overrides. START.bat / bootstrap auto-generate
# real secrets under ./data — do NOT commit your real .env.
FINAGENT_HOST=0.0.0.0
FINAGENT_PORT=8000
FINAGENT_DATA_DIR=./data
FINAGENT_DATABASE_URL=sqlite+aiosqlite:///finagent.db
# Placeholders only — replace locally; never commit real keys
FINAGENT_SECRET_KEY=dev-change-me-use-fernet-generate-key
FINAGENT_JWT_SECRET=dev-change-me-long-random-jwt-secret-string
